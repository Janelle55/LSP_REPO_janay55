The Template Method pattern is used by the abstract Report class. The generateReport() method defines the fixed workflow: loadData(), formatHeader(), formatBody(), and formatFooter(). StudentReport and CourseReport provide their own implementations of those steps while still following the same report generation process. The Driver uses a List<Report> and calls generateReport() on each object, which demonstrates polymorphism.